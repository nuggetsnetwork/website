import React from 'react';

import { Navbar, Nav, NavItem } from 'reactstrap';

import SourceLink from 'components/SourceLink';

const Footer = () => {
  return (
    <Navbar>
      <Nav navbar>
        <NavItem>
          If you see nothing in page then Click on the hamburger icon on top left of your screen and click on Netflix
        </NavItem>
      </Nav>
    </Navbar>
  );
};

export default Footer;
